class AboutController {
    // If some interaction is neded, it would be
    // defined here.
}

// No need of dependencies, but in here we would inject
// any service that we need.
AboutController.$inject = [];

// Export the class to be used somewhere else
export default AboutController;